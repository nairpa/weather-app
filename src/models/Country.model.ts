export interface EndpointCountry {
    id: number,
    name: string,
    latitude: number,
    longitude: number,
    elevation: number,
    feature_code: string,
    country_code: string,
    country: string,
}

export interface Country {
    id: number,
    name: string,
    latitude: number,
    longitude: number,
    country: string,
}